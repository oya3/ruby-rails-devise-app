%p#notice= notice

%p
%h1 Show train_route
%table.table.table-bordered
  %tbody
    %tr
      %td Code
      %td= @train_route.code
    %tr
      %td Name
      %td= @train_route.name

%p
%big
  %b Stations:
%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Code
      %th Name

  %tbody
  - @train_route.train_route_stations.order('row_oder').each do |train_route_station|
    - station = Staion.find(train_route_station.station)
    %tr
      %td= station.code
      %td= station.name

%p
= link_to 'Edit', edit_train_route_path(@train_route)
\|
= link_to 'Back', train_routes_path
