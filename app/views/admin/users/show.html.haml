%h1 Users#show
%p Find me in app/views/users/show.html.haml

.container
  %table.table.table-striped.table-bordered
    %thead
      %tr
        %th item
        %th value
    %tbody
      %tr
        %td id
        %td= @user.id
      %tr
        %td username
        %td= @user.username
      %tr
        %td email
        %td= @user.email
      %tr
        %td encrypted_password
        %td= @user.encrypted_password

  / FIXME: edit_user_registration_path はログインしているユーザしか変更できない
  = link_to 'Edit', edit_admin_user_path(@user)
  \|
  = link_to 'Back', admin_users_path
